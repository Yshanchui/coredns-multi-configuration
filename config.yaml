# CoreDNS Multi-Cluster Configuration Manager

server:
  host: "0.0.0.0"
  port: 80

auth:
  username: "admin"
  password: "admin123"
  jwt_secret: "coredns-manager-secret-key-change-me"

data_dir: "./data"
log_level: "info"
